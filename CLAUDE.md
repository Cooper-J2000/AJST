# CLAUDE.md — 仅入口指针（本文件不存放任何规则）

规则与约定的**唯一维护处是 `AGENTS.md`**，请直接读它。本机专属信息（路径/端口/服务/
凭据位置）在 `AGENTS.local.md`，若存在则先读。

**不要在本文件里添加内容**：任何规则、命令、路径、说明都写进 `AGENTS.md` 的对应小节，
这里只保留指向。`scripts/preflight.sh` 第 9 组会检查本文件是否仍是薄指针
（≤15 行、且不含二级标题），被塞入正文会报警告。
