# AJST 后端依赖（Python >= 3.10）
Flask>=3.0
flask-cors>=4.0
SQLAlchemy>=2.0
psycopg2-binary>=2.9
Werkzeug>=3.0
numpy>=1.26
scipy>=1.11
astropy>=6.0

# 银河系消光改正（CSFD 尘埃图 + P92 模型）
dustmaps>=1.0
dust_extinction>=1.4

# hostfit 路由启动时加载（pcigale.ini.spec 按开关裁剪），为硬依赖
configobj>=5.0

# ── 可选：余辉拟合功能（体积较大，不需要拟合可不装）──
# VegasAfterglow[mcmc]>=2.0
# corner>=2.2
# matplotlib>=3.8

# ── 可选：宿主星系 SED 拟合（宿主星系 tab；模型数据 ~3.7GB 随包附带）──
# pcigale>=2025.0
# matplotlib>=3.8
# 安装后需运行一次 scripts/fetch_svo_filters.py 注册滤光片曲线
# 注：emcee/h5py 由 VegasAfterglow[mcmc] 附带安装
